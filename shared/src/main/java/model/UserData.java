package model;

// create record classes and add them to the shared module that
// represent the classes used for the chess application's core data objects
public record UserData(String username, String password, String email){
}
